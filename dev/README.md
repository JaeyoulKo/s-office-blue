# Dev task workflow

각 작업자는 자신의 `dev/<task-id>/` 폴더만 수정한다.

1. `TASK.md`를 읽고 `work/`에 후보 결과를 만든다.
2. 합성 데이터로 확인하고 검토 메모를 남긴다.
3. 승인된 파일만 TASK에 적힌 승격 대상 경로로 옮긴다.
4. `work/`는 작업 이력이고 `skills/`, `main_service/`, `experiments/`가 사용 가능한 원본이다.

폴더가 업무 소유권을 나누며 Git 커밋과 PR은 검토 가능한 작은 변경 이력을 남긴다.
